import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.leonardodiazvidal.audioprocmvp',
  appName: 'Audio Proc MVP',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
